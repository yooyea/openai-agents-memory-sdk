{
  "name": "openai-agents-memory-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @agent-studio/web",
    "build:web": "npm run build --workspace @agent-studio/web",
    "typecheck:web": "npm run typecheck --workspace @agent-studio/web"
  }
}
